import { c as create_custom_element, S as SvelteComponent, i as init, f as flush, s as safe_not_equal, a as append_styles, n as noop, d as detach, k as attr, b as set_style, e as insert, g as append, h as element, o as onMount, l as onDestroy, r as binding_callbacks } from './chunks/index-DR_90iw3.js';

/* src/lib/Components/webcomponents/ARVRAvatar.wc.svelte generated by Svelte v4.2.20 */

function add_css(target) {
	append_styles(target, "svelte-ghhctt", ".arvr-avatar.svelte-ghhctt{display:inline-block;border:2px solid #e2e8f0;border-radius:8px;overflow:hidden;background:#fff}canvas.svelte-ghhctt{display:block;width:100%;height:100%}");
}

function create_fragment(ctx) {
	let div;
	let canvas;

	return {
		c() {
			div = element("div");
			canvas = element("canvas");
			attr(canvas, "width", /*canvasWidth*/ ctx[4]);
			attr(canvas, "height", /*canvasHeight*/ ctx[3]);
			attr(canvas, "class", "svelte-ghhctt");
			attr(div, "class", "arvr-avatar svelte-ghhctt");
			set_style(div, "width", /*width*/ ctx[0]);
			set_style(div, "height", /*height*/ ctx[1]);
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, canvas);
			/*canvas_binding*/ ctx[11](canvas);
		},
		p(ctx, [dirty]) {
			if (dirty & /*canvasWidth*/ 16) {
				attr(canvas, "width", /*canvasWidth*/ ctx[4]);
			}

			if (dirty & /*canvasHeight*/ 8) {
				attr(canvas, "height", /*canvasHeight*/ ctx[3]);
			}

			if (dirty & /*width*/ 1) {
				set_style(div, "width", /*width*/ ctx[0]);
			}

			if (dirty & /*height*/ 2) {
				set_style(div, "height", /*height*/ ctx[1]);
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) {
				detach(div);
			}

			/*canvas_binding*/ ctx[11](null);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let showInfoBool;
	let canvasWidth;
	let canvasHeight;
	let { userId = '' } = $$props;
	let { modelUrl = '/models/default-avatar.glb' } = $$props;
	let { skinTone = '#f0d5a8' } = $$props;
	let { bodyType = 'average' } = $$props;
	let { width = '300px' } = $$props;
	let { height = '400px' } = $$props;
	let { showInfo = 'true' } = $$props;
	let canvasElement;
	let ctx = null;

	onMount(() => {
		$$invalidate(10, ctx = canvasElement.getContext('2d'));

		if (ctx) {
			renderAvatar();
		}
	});

	function renderAvatar() {
		if (!ctx) return;

		// Clear canvas
		ctx.clearRect(0, 0, canvasWidth, canvasHeight);

		// Background
		$$invalidate(10, ctx.fillStyle = '#f0f0f0', ctx);

		ctx.fillRect(0, 0, canvasWidth, canvasHeight);

		// Avatar silhouette (simplified 2D representation)
		// Head
		$$invalidate(10, ctx.fillStyle = skinTone, ctx);

		ctx.beginPath();
		ctx.arc(canvasWidth / 2, canvasHeight / 3, 50, 0, Math.PI * 2);
		ctx.fill();

		// Body
		$$invalidate(10, ctx.fillStyle = '#4a5568', ctx);

		ctx.fillRect(canvasWidth / 2 - 40, canvasHeight / 3 + 30, 80, 100);

		// Arms
		ctx.fillRect(canvasWidth / 2 - 70, canvasHeight / 3 + 40, 25, 80);

		ctx.fillRect(canvasWidth / 2 + 45, canvasHeight / 3 + 40, 25, 80);

		// Legs
		ctx.fillRect(canvasWidth / 2 - 35, canvasHeight / 3 + 130, 25, 100);

		ctx.fillRect(canvasWidth / 2 + 10, canvasHeight / 3 + 130, 25, 100);

		// Label
		if (showInfoBool) {
			$$invalidate(10, ctx.fillStyle = '#000', ctx);
			$$invalidate(10, ctx.font = '14px sans-serif', ctx);
			$$invalidate(10, ctx.textAlign = 'center', ctx);
			ctx.fillText(`Avatar: ${userId || 'Guest'}`, canvasWidth / 2, canvasHeight - 20);
			ctx.fillText(`Type: ${bodyType}`, canvasWidth / 2, canvasHeight - 5);
		}

		dispatchEvent(new CustomEvent('rendered', { detail: { userId, modelUrl } }));
	}

	onDestroy(() => {
		$$invalidate(10, ctx = null);
	});

	function canvas_binding($$value) {
		binding_callbacks[$$value ? 'unshift' : 'push'](() => {
			canvasElement = $$value;
			$$invalidate(2, canvasElement);
		});
	}

	$$self.$$set = $$props => {
		if ('userId' in $$props) $$invalidate(5, userId = $$props.userId);
		if ('modelUrl' in $$props) $$invalidate(6, modelUrl = $$props.modelUrl);
		if ('skinTone' in $$props) $$invalidate(7, skinTone = $$props.skinTone);
		if ('bodyType' in $$props) $$invalidate(8, bodyType = $$props.bodyType);
		if ('width' in $$props) $$invalidate(0, width = $$props.width);
		if ('height' in $$props) $$invalidate(1, height = $$props.height);
		if ('showInfo' in $$props) $$invalidate(9, showInfo = $$props.showInfo);
	};

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*showInfo*/ 512) {
			showInfoBool = showInfo === 'true';
		}

		if ($$self.$$.dirty & /*width*/ 1) {
			$$invalidate(4, canvasWidth = parseInt(width) || 300);
		}

		if ($$self.$$.dirty & /*height*/ 2) {
			$$invalidate(3, canvasHeight = parseInt(height) || 400);
		}

		if ($$self.$$.dirty & /*ctx, skinTone, bodyType, userId*/ 1440) {
			if (ctx && (skinTone || bodyType || userId)) {
				renderAvatar();
			}
		}
	};

	return [
		width,
		height,
		canvasElement,
		canvasHeight,
		canvasWidth,
		userId,
		modelUrl,
		skinTone,
		bodyType,
		showInfo,
		ctx,
		canvas_binding
	];
}

class ARVRAvatar_wc extends SvelteComponent {
	constructor(options) {
		super();

		init(
			this,
			options,
			instance,
			create_fragment,
			safe_not_equal,
			{
				userId: 5,
				modelUrl: 6,
				skinTone: 7,
				bodyType: 8,
				width: 0,
				height: 1,
				showInfo: 9
			},
			add_css
		);
	}

	get userId() {
		return this.$$.ctx[5];
	}

	set userId(userId) {
		this.$$set({ userId });
		flush();
	}

	get modelUrl() {
		return this.$$.ctx[6];
	}

	set modelUrl(modelUrl) {
		this.$$set({ modelUrl });
		flush();
	}

	get skinTone() {
		return this.$$.ctx[7];
	}

	set skinTone(skinTone) {
		this.$$set({ skinTone });
		flush();
	}

	get bodyType() {
		return this.$$.ctx[8];
	}

	set bodyType(bodyType) {
		this.$$set({ bodyType });
		flush();
	}

	get width() {
		return this.$$.ctx[0];
	}

	set width(width) {
		this.$$set({ width });
		flush();
	}

	get height() {
		return this.$$.ctx[1];
	}

	set height(height) {
		this.$$set({ height });
		flush();
	}

	get showInfo() {
		return this.$$.ctx[9];
	}

	set showInfo(showInfo) {
		this.$$set({ showInfo });
		flush();
	}
}

customElements.get("sl-arvr-avatar")||customElements.define("sl-arvr-avatar", create_custom_element(ARVRAvatar_wc, {"userId":{},"modelUrl":{},"skinTone":{},"bodyType":{},"width":{},"height":{},"showInfo":{}}, [], [], true));

export { ARVRAvatar_wc as default };
//# sourceMappingURL=arvr-avatar.js.map
