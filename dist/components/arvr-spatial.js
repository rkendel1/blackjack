import { c as create_custom_element, S as SvelteComponent, i as init, s as safe_not_equal, f as flush, a as append_styles, n as noop, d as detach, w as run_all, t as toggle_class, m as set_data, e as insert, g as append, u as listen, h as element, j as space, p as text, k as attr, o as onMount, l as onDestroy, r as binding_callbacks } from './chunks/index-DR_90iw3.js';

/* src/lib/Components/webcomponents/ARVRSpatial.wc.svelte generated by Svelte v4.2.20 */

function add_css(target) {
	append_styles(target, "svelte-n4ojp9", ".arvr-spatial.svelte-n4ojp9{display:inline-block;border:2px solid #2d3748;border-radius:8px;overflow:hidden;background:#1a1a1a}canvas.svelte-n4ojp9{display:block}canvas.interactive.svelte-n4ojp9{cursor:move}.object-info.svelte-n4ojp9{background:rgba(0, 0, 0, 0.9);color:white;padding:8px;font-size:12px;text-align:center;border-top:1px solid #374151}.object-id.svelte-n4ojp9{color:#9ca3af;font-size:10px;margin-left:8px}strong.svelte-n4ojp9{text-transform:capitalize}");
}

// (148:2) {#if objectId}
function create_if_block(ctx) {
	let span;
	let t0;
	let t1;

	return {
		c() {
			span = element("span");
			t0 = text("#");
			t1 = text(/*objectId*/ ctx[0]);
			attr(span, "class", "object-id svelte-n4ojp9");
		},
		m(target, anchor) {
			insert(target, span, anchor);
			append(span, t0);
			append(span, t1);
		},
		p(ctx, dirty) {
			if (dirty & /*objectId*/ 1) set_data(t1, /*objectId*/ ctx[0]);
		},
		d(detaching) {
			if (detaching) {
				detach(span);
			}
		}
	};
}

function create_fragment(ctx) {
	let div1;
	let canvas;
	let t0;
	let div0;
	let strong;
	let t1;
	let t2;
	let mounted;
	let dispose;
	let if_block = /*objectId*/ ctx[0] && create_if_block(ctx);

	return {
		c() {
			div1 = element("div");
			canvas = element("canvas");
			t0 = space();
			div0 = element("div");
			strong = element("strong");
			t1 = text(/*objectType*/ ctx[1]);
			t2 = space();
			if (if_block) if_block.c();
			attr(canvas, "width", "200");
			attr(canvas, "height", "200");
			attr(canvas, "class", "svelte-n4ojp9");
			toggle_class(canvas, "interactive", /*interactiveBool*/ ctx[3]);
			attr(strong, "class", "svelte-n4ojp9");
			attr(div0, "class", "object-info svelte-n4ojp9");
			attr(div1, "class", "arvr-spatial svelte-n4ojp9");
		},
		m(target, anchor) {
			insert(target, div1, anchor);
			append(div1, canvas);
			/*canvas_binding*/ ctx[13](canvas);
			append(div1, t0);
			append(div1, div0);
			append(div0, strong);
			append(strong, t1);
			append(div0, t2);
			if (if_block) if_block.m(div0, null);

			if (!mounted) {
				dispose = [
					listen(canvas, "mousedown", /*handleMouseDown*/ ctx[4]),
					listen(canvas, "mousemove", /*handleMouseMove*/ ctx[5]),
					listen(canvas, "mouseup", /*handleMouseUp*/ ctx[6]),
					listen(canvas, "mouseleave", /*handleMouseUp*/ ctx[6])
				];

				mounted = true;
			}
		},
		p(ctx, [dirty]) {
			if (dirty & /*interactiveBool*/ 8) {
				toggle_class(canvas, "interactive", /*interactiveBool*/ ctx[3]);
			}

			if (dirty & /*objectType*/ 2) set_data(t1, /*objectType*/ ctx[1]);

			if (/*objectId*/ ctx[0]) {
				if (if_block) {
					if_block.p(ctx, dirty);
				} else {
					if_block = create_if_block(ctx);
					if_block.c();
					if_block.m(div0, null);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) {
				detach(div1);
			}

			/*canvas_binding*/ ctx[13](null);
			if (if_block) if_block.d();
			mounted = false;
			run_all(dispose);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let interactiveBool;
	let positionArray;
	let rotationArray;
	let scaleArray;
	let { objectId = '' } = $$props;
	let { objectType = 'cube' } = $$props;
	let { position = '0,0,-2' } = $$props;
	let { rotation = '0,0,0,1' } = $$props;
	let { scale = '1,1,1' } = $$props;
	let { color = '#4299e1' } = $$props;
	let { interactive = 'true' } = $$props;
	let canvasElement;
	let ctx = null;
	let isDragging = false;
	let dragStartX = 0;
	let dragStartY = 0;

	onMount(() => {
		$$invalidate(12, ctx = canvasElement.getContext('2d'));

		if (ctx) {
			renderObject();
		}

		dispatchEvent(new CustomEvent('object-placed',
		{
				detail: {
					objectId,
					objectType,
					position: positionArray,
					rotation: rotationArray,
					scale: scaleArray
				}
			}));
	});

	function renderObject() {
		if (!ctx) return;
		const width = 200;
		const height = 200;

		// Clear canvas
		ctx.clearRect(0, 0, width, height);

		// Background
		$$invalidate(12, ctx.fillStyle = '#1a1a1a', ctx);

		ctx.fillRect(0, 0, width, height);

		// Draw grid
		$$invalidate(12, ctx.strokeStyle = '#333', ctx);

		$$invalidate(12, ctx.lineWidth = 1, ctx);

		for (let i = 0; i <= width; i += 20) {
			ctx.beginPath();
			ctx.moveTo(i, 0);
			ctx.lineTo(i, height);
			ctx.stroke();
		}

		for (let i = 0; i <= height; i += 20) {
			ctx.beginPath();
			ctx.moveTo(0, i);
			ctx.lineTo(width, i);
			ctx.stroke();
		}

		// Draw object (simple 2D representation)
		const centerX = width / 2 + positionArray[0] * 20;

		const centerY = height / 2 + positionArray[1] * 20;
		const size = 40 * scaleArray[0];
		$$invalidate(12, ctx.fillStyle = color, ctx);
		$$invalidate(12, ctx.strokeStyle = '#fff', ctx);
		$$invalidate(12, ctx.lineWidth = 2, ctx);

		if (objectType === 'cube') {
			// Draw cube (isometric view)
			ctx.beginPath();

			ctx.moveTo(centerX, centerY - size / 2);
			ctx.lineTo(centerX + size / 2, centerY - size / 4);
			ctx.lineTo(centerX + size / 2, centerY + size / 4);
			ctx.lineTo(centerX, centerY + size / 2);
			ctx.lineTo(centerX - size / 2, centerY + size / 4);
			ctx.lineTo(centerX - size / 2, centerY - size / 4);
			ctx.closePath();
			ctx.fill();
			ctx.stroke();
		} else if (objectType === 'sphere') {
			// Draw sphere
			ctx.beginPath();

			ctx.arc(centerX, centerY, size / 2, 0, Math.PI * 2);
			ctx.fill();
			ctx.stroke();
		} else {
			// Default: draw cylinder
			ctx.fillRect(centerX - size / 2, centerY - size / 2, size, size);

			ctx.strokeRect(centerX - size / 2, centerY - size / 2, size, size);
		}

		// Draw label
		$$invalidate(12, ctx.fillStyle = '#fff', ctx);

		$$invalidate(12, ctx.font = '10px sans-serif', ctx);
		$$invalidate(12, ctx.textAlign = 'center', ctx);
		ctx.fillText(objectId || objectType, centerX, centerY + size / 2 + 15);
	}

	function handleMouseDown(e) {
		if (!interactiveBool) return;
		isDragging = true;
		dragStartX = e.offsetX;
		dragStartY = e.offsetY;
	}

	function handleMouseMove(e) {
		if (!isDragging) return;
		const deltaX = (e.offsetX - dragStartX) / 20;
		const deltaY = (e.offsetY - dragStartY) / 20;
		positionArray[0] += deltaX;
		positionArray[1] -= deltaY; // Invert Y for natural movement
		$$invalidate(7, position = positionArray.join(','));
		dragStartX = e.offsetX;
		dragStartY = e.offsetY;
		renderObject();

		dispatchEvent(new CustomEvent('object-moved',
		{
				detail: { objectId, position: positionArray }
			}));
	}

	function handleMouseUp() {
		isDragging = false;
	}

	onDestroy(() => {
		$$invalidate(12, ctx = null);
	});

	function canvas_binding($$value) {
		binding_callbacks[$$value ? 'unshift' : 'push'](() => {
			canvasElement = $$value;
			$$invalidate(2, canvasElement);
		});
	}

	$$self.$$set = $$props => {
		if ('objectId' in $$props) $$invalidate(0, objectId = $$props.objectId);
		if ('objectType' in $$props) $$invalidate(1, objectType = $$props.objectType);
		if ('position' in $$props) $$invalidate(7, position = $$props.position);
		if ('rotation' in $$props) $$invalidate(8, rotation = $$props.rotation);
		if ('scale' in $$props) $$invalidate(9, scale = $$props.scale);
		if ('color' in $$props) $$invalidate(10, color = $$props.color);
		if ('interactive' in $$props) $$invalidate(11, interactive = $$props.interactive);
	};

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*interactive*/ 2048) {
			$$invalidate(3, interactiveBool = interactive === 'true');
		}

		if ($$self.$$.dirty & /*position*/ 128) {
			positionArray = position.split(',').map(Number);
		}

		if ($$self.$$.dirty & /*rotation*/ 256) {
			rotationArray = rotation.split(',').map(Number);
		}

		if ($$self.$$.dirty & /*scale*/ 512) {
			scaleArray = scale.split(',').map(Number);
		}

		if ($$self.$$.dirty & /*ctx, position, rotation, scale, color*/ 6016) {
			if (ctx && (position || rotation || scale || color)) {
				renderObject();
			}
		}
	};

	return [
		objectId,
		objectType,
		canvasElement,
		interactiveBool,
		handleMouseDown,
		handleMouseMove,
		handleMouseUp,
		position,
		rotation,
		scale,
		color,
		interactive,
		ctx,
		canvas_binding
	];
}

class ARVRSpatial_wc extends SvelteComponent {
	constructor(options) {
		super();

		init(
			this,
			options,
			instance,
			create_fragment,
			safe_not_equal,
			{
				objectId: 0,
				objectType: 1,
				position: 7,
				rotation: 8,
				scale: 9,
				color: 10,
				interactive: 11
			},
			add_css
		);
	}

	get objectId() {
		return this.$$.ctx[0];
	}

	set objectId(objectId) {
		this.$$set({ objectId });
		flush();
	}

	get objectType() {
		return this.$$.ctx[1];
	}

	set objectType(objectType) {
		this.$$set({ objectType });
		flush();
	}

	get position() {
		return this.$$.ctx[7];
	}

	set position(position) {
		this.$$set({ position });
		flush();
	}

	get rotation() {
		return this.$$.ctx[8];
	}

	set rotation(rotation) {
		this.$$set({ rotation });
		flush();
	}

	get scale() {
		return this.$$.ctx[9];
	}

	set scale(scale) {
		this.$$set({ scale });
		flush();
	}

	get color() {
		return this.$$.ctx[10];
	}

	set color(color) {
		this.$$set({ color });
		flush();
	}

	get interactive() {
		return this.$$.ctx[11];
	}

	set interactive(interactive) {
		this.$$set({ interactive });
		flush();
	}
}

customElements.get("sl-arvr-spatial")||customElements.define("sl-arvr-spatial", create_custom_element(ARVRSpatial_wc, {"objectId":{},"objectType":{},"position":{},"rotation":{},"scale":{},"color":{},"interactive":{}}, [], [], true));

export { ARVRSpatial_wc as default };
//# sourceMappingURL=arvr-spatial.js.map
